apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-infra.fullname" . }}
  labels:
    {{- include "kafka-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-infra.selectorLabels" . | nindent 4 }}
